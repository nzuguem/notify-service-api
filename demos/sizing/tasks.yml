version: 3

tasks:

  build-image/*:
    vars:
      VARIANT: '{{index .MATCH 0}}'
    desc: Build Image
    cmd: docker build -f demos/sizing/Dockerfile.{{.VARIANT}} -t sizing-{{.VARIANT}}-notify-service-api .