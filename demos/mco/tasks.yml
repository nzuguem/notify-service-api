version: 3

tasks:

  build-image/*:
    vars:
      VARIANT: '{{index .MATCH 0}}'
    desc: Build Image
    cmd: docker build -f demos/mco/Dockerfile.{{.VARIANT}} -t mco-{{.VARIANT}}-notify-service-api .
  
  run-image/*/*:
    deps:
      - build-image/v4
    vars:
      VARIANT: '{{index .MATCH 0}}'
      ENV: '{{index .MATCH 1}}'
    desc: Run Image
    cmd: docker run -it --net host $DOCKER_LOGS_OPTS  --rm  --env-file config-managment/{{.ENV}}.properties mco-{{.VARIANT}}-notify-service-api